{
  "framework": "nextjs",
  "ignoreCommand": "if [ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] || ! git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA^{commit}\" 2>/dev/null; then exit 1; fi; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- app components lib public scripts package.json package-lock.json next.config.mjs tsconfig.json vercel.json",
  "crons": [
    {
      "path": "/api/balance?refresh=1",
      "schedule": "0 9 * * *"
    }
  ]
}
