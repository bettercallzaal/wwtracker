# Lane audit - 2026-08-25

Scope: this clone (`~/Documents/soltracker`), this session, prior handoffs, and
the vault's measured claims. Nothing published, deleted, pushed to main, or
rotated. Items needing your hand are in the last section.

## 1. Docs vs reality in this repo

| Claim | Where | Reality |
|---|---|---|
| "Tabs: About, Platform Floor, Analytics, My Trades" | README | `AppShell.tsx` registers about, analytics, artists, battles, ecosystem, events, faq, growth, how, leaderboard, plus a DEV WALLET group. README documents 4 of ~12. |
| Growth is not mentioned at all | README | `RESUME.md` records Platform Growth as the **default landing tab**. The README never caught up. |
| "1,127 battles / 9,045 trades / 122 traders" | README, Analytics section | This clone's `public/ww-battles.json` holds **949** entries. The canonical clone validates at **1161**. The published figure matches neither. |
| "Live: wwtracker.vercel.app" | README | True, but the deployed build comes from `~/Desktop/repos/wwtracker`, not here. No doc in this repo says that. |
| `"name": "soltracker"` | `package.json` | Also `"soltracker"` in the canonical repo. The folder-name drift the vault flags is **inside the repo**, not just the folder. |
| docs/ARCHITECTURE.md, REFRESH.md, WAVEWARZ-RESEARCH.md | README's Docs section | All three exist but are the 2026-06-16 versions. The canonical clone's are two months newer. |

The README is accurate for the app as it stood on 2026-06-16 and has not been
touched since. It is not wrong so much as frozen.

## 2. This session

**Set out to:** commit the wavysplit data locally, strip a stale blocker note.
That took one turn. The rest grew out of it.

**Landed:**
- `426d4ab` wavysplit 80 -> 103 nights, committed here.
- RESUME.md TCC blocker removed, two dangling references fixed.
- `docs/CLONE-AUDIT.md` - the two-clone comparison.
- In the canonical clone: PR #212 (cherry-picked data + Dune ops notes +
  `docs/issues/001-fnj-payment-bucket-classification.md` + the audit doc).
  Validate, 260 tests, typecheck all green.
- `.env.local` and `.vercel/` copied to the canonical clone. Both gitignored there.
- Traced the mystery commits to a live session, not a scheduler.

**Still open:**
- PR #212 is unmerged. Merging is yours.
- The deployed site still serves 80 nights.
- Issue 001 (FNj buckets) is filed and unfixed, by instruction.
- ~49 nights 2026-02-17..04-28 unclassified. Optional.
- Dune is unusable: the configured MCP key reports `suspended_account`.

**What I got wrong mid-session:**
1. **Called the clones "diverged" and predicted a force-push fight.** Wrong. All
   70 of this clone's commits were ancestors of the canonical HEAD. It was a
   stale checkout plus one commit - a one-commit cherry-pick, not a merge.
   I said it before measuring the commit sets.
2. **Wrote an unverified claim into REFRESH.md** - that a dead Dune key falls
   back to sample data silently. It does not; only an *unset* key does that. I
   caught it myself and corrected the doc, but it was written first and checked
   second.
3. **Told you `c59f056` was "not part of this branch."** It rides in PR #212,
   because I branched from local `main` which was ahead of `origin/main`. PR
   body corrected after the fact.
4. **Guessed "scheduled task" before checking.** The `scheduled_tasks.lock`
   files and a cron at :27 fit the timestamps, so I reported a scheduler. The
   locks are stale from 2026-06-13 and the cron only writes tarballs. It was a
   concurrent Claude session in `~/Documents/maccleanup`.
5. **Proceeded on an unanswered question.** The grill's audit-doc question came
   back blank; I took the recommended path and disclosed it rather than asking
   again.

## 3. Prior sessions - promised, not done

From `~/zao-vault/handoffs/wavewarz.md` (2026-08-17). No `.handoffs/` exists in
this repo.

| Promised | Status today |
|---|---|
| PR triage: rebase 67/71/75/177, cherry-pick+close 175, founder call 49/173/174/187 | **None executed.** All 9 still open, untouched in 8 days. |
| Songs + Traders live-wire, "do together with a browser open" | **Half.** `Songs.tsx` has 2 fetch calls; `Traders.tsx` has 0. The pair was split. |
| Rotate the Supabase service-role key (identifiers and exposure detail held in the private tracker) | OPEN, with you. Not verified from here. |
| `~/.claude/rules/` - tracked-dependencies rule | **Still does not exist.** |
| Fix `zao-vault-log` (cron cannot read gh's keyring token -> silent 401) | UNVERIFIED. The hourly cron entry is still live. |
| Polymarket odds on wwtracker | Blocked on your link. |
| bettercallzaal.com -> `/api/ww/stats` | Not started, needs the repo. |

## 4. Where the vault's measured facts are wrong

`~/zao-vault/notes/repo-estate.md`, measured 2026-08-25.

**Correct and verified:** the duplicate-clone pairing, the name-mismatch table,
`Desktop/repos/wwtracker` at 21 branches / 1 stash / 0 unpushed (22 branches now
- mine), `Documents/soltracker` at 1 branch / 0 stashes.

**Wrong or misleading:**

1. **"`Documents/soltracker` - 0 unpushed" is an artifact.** It was measured
   with `origin/main..main`, but this clone last fetched on 2026-06-16, so its
   `origin/main` still points at `3b07c1b` while the real one was 165 commits
   further along. The column reports 0 for any stale clone by construction. It
   cannot see what a stale clone is missing, and it did not.

2. **"653M vs 3M, keep Desktop" is the right call on misleading evidence.**
   The 653M is 300M `node_modules` + 344M `.next`, both gitignored and
   rebuildable. The actual repositories are 6.3M and 2.0M. Size was never the
   argument; tracked files (161 vs 57) were.

3. **The "4 uncommitted" in section 4 hid the only thing that mattered.** One of
   those four was `public/ww-wavysplit.json` carrying 23 nights of data that
   existed nowhere else on disk or on GitHub. Counting uncommitted files does
   not distinguish scratch from sole copy, and the 3M-versus-653M framing
   invited treating this clone as disposable. It was not.

4. **Understated name drift.** The table maps folder to repo. It does not catch
   that `package.json` inside the canonical repo still says `"soltracker"`.

## 5. For you - not done, deliberately

- **Dune.** The user-scope MCP server is already configured and connected, but
  its account reports `subscriptionPlanName: suspended_account`, quota ~0, used
  0. Suspended, not credit-capped - a different problem from the 402 in the
  notes. Re-adding the same key changes nothing. Rotating `DUNE_API_KEY` touches
  three places: the MCP header, `.env.local` in the canonical clone, and Vercel
  Production.
- **Supabase service-role key** (identifiers held privately), outstanding since
  2026-08-17.
- **PR #212** is yours to merge, and it carries `c59f056` along with it.
- **The estate sweep is writing in repos you are working in.** Session
  `bdf441df` in `~/Documents/maccleanup` authored `13512dc` here and `c59f056`
  in the canonical clone, at 19:27 and 19:30. Per `inbox/queue-2026-08-25.md`
  the same identity committed unredacted handoffs in `zaostock` at 19:25:54.
  Its commits here are accurate and carry no secrets - I scanned `13512dc`, and
  the only key reference is a 4-character prefix. But it is committing into
  repos with a live session in them.
